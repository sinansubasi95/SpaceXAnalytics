query launchesPast($limit: Int!) {
    launchesPast(limit: $limit) {
        mission_name
        launch_date_utc
        rocket {
            rocket_name
        }
        links {
            mission_patch
        }
        id
        details
    }
}